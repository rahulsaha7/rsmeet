import MobileView from "./MobileView";

import "./Dashboard.css";

const Dashboard = ({ authTokenValues }) => {
  return <MobileView authTokenValues={authTokenValues} />;
};

export default Dashboard;
